using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class FeedbackIntruction : MonoBehaviour
{

    public TMPro.TMP_Text feedbackText;
    public GameObject golie;

    // Start is called before the first frame update
    void Start()
    {

       // feedbackText.text = "";

    }

    // Update is called once per frame
    void Update()
    {

       
        //golie.GetComponent<BallSpawner>().feedbackText = feedbackText;
    }
}
