using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TriggerToPosition : MonoBehaviour
{

    public GameObject cube;
    public GameObject centerHand;
    void Update()
    {

     /*   Debug.Log("cube position: " + cube.transform.position);
        Debug.Log("trigger position: " + centerHand.transform.position);

        Debug.Log("cube position x: " + cube.transform.position.x);
        Debug.Log("trigger position x: " + centerHand.transform.position.x);

        Debug.Log("cube position y: " + cube.transform.position.y);
        Debug.Log("trigger position y: " + centerHand.transform.position.y);

        Debug.Log("cube position z: " + cube.transform.position.z);
        Debug.Log("trigger position z: " + centerHand.transform.position.z);

        Debug.Log("rounded x" + System.Math.Round(cube.transform.position.x, 1));
        Debug.Log("rounded y" + System.Math.Round(cube.transform.position.y, 1));
        Debug.Log("rounded z" + System.Math.Round(cube.transform.position.z, 1));

        Debug.Log("hand x" + System.Math.Round(centerHand.transform.position.x, 1));
        Debug.Log("hand y" + System.Math.Round(centerHand.transform.position.y, 1));
        Debug.Log("hand z" + System.Math.Round(centerHand.transform.position.z, 1));




        if (System.Math.Round(cube.transform.position.x, 1) == System.Math.Round(centerHand.transform.position.x, 1) && System.Math.Round(cube.transform.position.y, 1) == System.Math.Round(centerHand.transform.position.y, 1) && System.Math.Round(cube.transform.position.z, 1) == System.Math.Round(centerHand.transform.position.z, 1))
        {
            Debug.Log("Acertou");
        }
        */
    }
}
